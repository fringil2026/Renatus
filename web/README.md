# web/ — customer web app (scaffold)

The self-serve product surface: **Next.js + TypeScript**. The funnel from PRODUCT-PLAN §4 — signup →
diagnostic → ownership verification → concept boards → prototype → edits → review → publish/launch.
Status-driven and forgiving (carry over the operator dashboard's render-invariant lesson: never
destroy in-flight UI state on poll).

**Status:** scaffold only — not yet implemented. Built in Phase 3 (PRODUCT-PLAN §7).
