Drop client brand assets here:
  logo.svg        (vector — Claude wires it into the header + favicon)
  *.jpg / *.png   (photography; Claude references by filename and writes alt text)
Provide the logo as SVG when possible, not a scraped PNG (workflow §2.6).
